{
  "name": "hello-deutsch-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html style.css script.js public/* dist/ 2>/dev/null || true"
  },
  "dependencies": {}
}
